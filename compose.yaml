######
# Compose fragment file for the HoloROS driver
######

services:
  foxglove-studio:
    image: registry.gitlab.sitcore.net/xr-autonomy/champ/foxglove-studio:${TAG:-latest}
    build:
      context: ./
      args: 
        TAG: ${TAG:-latest}
      tags:
        - registry.gitlab.sitcore.net/xr-autonomy/champ/foxglove-studio:latest
      x-bake:
        platforms:
          - linux/amd64
          - linux/arm64
    # env_file:
    # - ./.env
    # - path: ../.env
    #   required: false
    # volumes:
    # - ./foxglove-layout.json:/foxglove/default-layout.json
    ports:
    - "8080:8080"
